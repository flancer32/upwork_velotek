/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Velotek_Shared_Defaults {
    NAME = '@flancer32/upwork_velotek';

    LOGS_AGG_URL = 'https://console.wiredgeese.com/timed/';

    constructor() {
        Object.freeze(this);
    }
}
